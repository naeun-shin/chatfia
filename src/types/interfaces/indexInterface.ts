/* index 페이지 props 타입 */
export interface TextImageProps {
  title: string;
  text: string;
  image: string;
  isTextLeft: boolean;
}

export interface ImageTextProps {
  title: string;
  text: string;
  image: string;
  isTextLeft: boolean;
}
